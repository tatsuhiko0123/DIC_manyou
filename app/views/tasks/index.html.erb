<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タスク名</th>
    <th>タスク</th>
  </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.title %></td>
      <td><%= task.content %></td>
      <td><%= link_to '詳細を確認', task_path(task.id) %></td>
    </tr>
  <% end %>
</table>

